<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.media.tecinfo.afinal">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SaveLogin_Activity"
            android:label="Tela de Login">
">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".Registro_Activity" />
        <activity android:name=".Dados_Activity" />
        <activity android:name=".Calculadora_Activity" />
        <activity android:name=".Chamada_Activity" />

        <activity android:name=".Cadastro_Activity" />

        <activity android:name=".Login_Activity" />

        <activity android:name=".Media_Activity"></activity>
    </application>

</manifest>